<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chatapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/mess"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/mess"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Ui_Activities.PhotoChat"></activity>
        <activity
            android:name=".Ui_Activities.ChatRoom"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".Ui_Activities.HomeActivity" />
        <activity android:name=".Ui_Activities.SignupPage" />
        <activity android:name=".Ui_Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>